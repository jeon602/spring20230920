-- 주석
# 주석
select * from w3schools.customers;
select * from w3schools.employees; -- 보통 커서가 있는 쿼리가 실행된다 둘다
select *
from w3schools.customers; -- 커서를 옮겨가면서 ctrl + enter 실행
-- sql은 대소문자 구분안함 섞어서 써도 무관. 관습: 키워드는 대문자, 나머지는 소문자 (유동적으로)
-- 이미 존재하는 데이터를 찾는 방법 부터
